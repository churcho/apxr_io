<%
experiment_url = Routes.experiment_url(Endpoint, :show, @project, @release, @experiment, [])
%>

<p>
  <a href="<%= raw experiment_url %>">Inspect here</a> or copy and paste the following link into your web browser: <%= raw experiment_url %>
</p>

<p>
  ---<br>
  ApproximateReality
</p>